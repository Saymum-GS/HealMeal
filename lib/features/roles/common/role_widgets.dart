// Placeholder for role widgets used across the app.
// Add actual widget implementations as needed.

import 'package:flutter/material.dart';

// Example widget (can be removed later)
class RoleWidgetPlaceholder extends StatelessWidget {
  const RoleWidgetPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox.shrink();
  }
}
